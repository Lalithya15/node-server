name: Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    
    steps:
      - name: Checkout code
        # Fetches your repository code
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        # Logs into ghcr.io using the provided GitHub Token
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set image tag (lowercase and dynamic)
        id: meta
        run: |
          # Use 'tr' in the shell to guarantee the repository owner name is lowercase
          OWNER_LOWERCASE=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          TAG="ghcr.io/$OWNER_LOWERCASE/nextapp:latest"
          
          # Output the final, correctly formatted tag
          echo "tag=$TAG" >> $GITHUB_OUTPUT

      - name: Build Docker image
        run: |
          # Builds the image using the securely-formatted tag from the step output
          docker build -t ${{ steps.meta.outputs.tag }} .

      - name: Push Docker image
        run: |
          # Pushes the image to GHCR
          docker push ${{ steps.meta.outputs.tag }}
